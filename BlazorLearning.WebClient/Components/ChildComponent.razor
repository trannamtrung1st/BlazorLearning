@inject ILogger<ChildComponent> Logger
@preservewhitespace true

<h3 @ref="H1Ref">ChildComponent</h3>
<p style="white-space: pre">
    @if (new Random().Next() % 2 == 0)
    {
        <text>
            Hello   1  space aloooo coococococo @(true ? "AA" : "B")
        </text>
    }
    else
    {
        <text>
            Hello   2  space aloooo coococococo @(true ? "AA" : "B")
        </text>
    }
</p>

@code {
    private ElementReference H1Ref;

    public void ChildMethod()
    {
        Logger.LogInformation($"Child method was called {H1Ref}");
    }
}
